version: 2
jobs:
  build:
    docker:
      - image: ubuntu:16.04
    steps:
      - checkout
      - run: sudo apt-get install clang libicu-dev libcurl4-openssl-dev wget unzip -y 
      - run:
          name: Print the Current Time
          command: date