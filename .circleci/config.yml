
version: 2
jobs:

  linux_swift_4:
    docker:
      - image: ibmcom/swift-ubuntu:4.0
    steps:
      - checkout
      - run: 
          name: Swift Test
          command: swift test
      - run:
          name: Swift build Release
          command: swift build -c release -Xswiftc -static-stdlib       

  linux_swift_4.0.2:
    docker:
      #- image: ibmcom/swift-ubuntu:4.0.2
      -image: swiftdocker/swift:4.0.2
    steps:
      - run: 
          name: Swift Test
          command: swift test
      - run:
          name: Swift build Release
          command: swift build -c release -Xswiftc -static-stdlib       

workflows:
  version: 2
  workflow:
    jobs:
      - linux_swift_4
      - linux_swift_4.0.2