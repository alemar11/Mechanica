version: 2
jobs:
  build:
    docker:
      - image: ibmcom/swift-ubuntu:4.0.2
    steps:
      - checkout
      - run:
          name: Swift build
          command: swift build
      - run:
          name: Print the Current Time
          command: date