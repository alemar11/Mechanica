version: 2
jobs:

  linux_swift_5:
    docker:
      - image: norionomura/swift-5.0-branch 
    steps:
      - checkout
      - run: 
          name: Swift Test
          command: swift test
      - run:
          name: Swift build Release
          command: swift build -c release -Xswiftc -static-stdlib     
workflows:
  version: 2
  workflow:
    jobs:
      - linux_swift_5

#docker: https://github.com/realm/SwiftLint/blob/master/circle.yml
